import prompt from 'prompt-sync'
const input = prompt()

function main() {
    const n = Number(input('Digite a quantidade de números: '))
    
    let i = 0

    while (n > i) {    
        const numbers = Number(input('Digite um número: '))
    }
}

main()